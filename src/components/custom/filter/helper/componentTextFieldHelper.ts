export interface TextFieldComponentProp {
  label: string;
  placeholder?: string;
  defaultValue?: string;
  isLabelTranslation?: boolean;
  isPlaceholderTranslation?: boolean;
}

export interface TextFieldComponentSummaryProp {
  summaryLabel: string;
  isSummaryLabelTranslation?: boolean;
}
