import { TableRowProps } from '@mui/material/TableRow';

export interface TableRowCompProps extends TableRowProps {
  // isTranslation?: boolean;
}

export const tableRowCompDefaultProps: TableRowCompProps = {
  // isTranslation: true,
};
