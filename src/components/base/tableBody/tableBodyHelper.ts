import { TableBodyProps } from '@mui/material/TableBody';

export interface TableBodyCompProps extends TableBodyProps {
  // isTranslation?: boolean;
}

export const tableBodyCompDefaultProps: TableBodyCompProps = {
  // isTranslation: true,
};
